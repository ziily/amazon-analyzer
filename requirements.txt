# =====================================================
# Amazon 产品竞争力分析 v3.1 - 完整依赖清单
# =====================================================
# 支持：Streamlit 应用 + CLIP 图片评分 + DINOv2 外观向量 + rembg 抠图
# 首次安装预计耗时：3-5 分钟，占用磁盘空间约 1.2GB
# =====================================================

# ---------- Web 界面与数据处理 ----------
streamlit>=1.30.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0
matplotlib>=3.7.0
Pillow>=10.0.0
requests>=2.31.0
pyarrow>=4.0.0           # 用于 parquet 缓存加速

# ---------- AI/ML 核心 (CLIP + DINOv2) ----------
# 如果机器没有 NVIDIA 显卡，PyTorch 会自动安装 CPU 版本（约 500MB）
# 如果报 CUDA 相关错误，可手动替换为：pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
transformers>=4.36.0     # 同时支持 CLIP 和 DINOv2 模型

# ---------- 🆕 去背景抠图（rembg 依赖链）----------
# rembg 核心库（自动下载 u2net 模型约 100MB，首次运行时会下载）
rembg>=2.0.50

# 必须安装 headless 版本，否则在 Linux 服务器（如 Streamlit Cloud）上会报错
opencv-python-headless>=4.8.0

# rembg 的 ONNX 推理后端（CPU 版，兼容性最好）
onnxruntime>=1.15.0

# ---------- 可选但推荐（提高 DINOv2 加载稳定性）----------
# 加速 transformers 模型加载
accelerate>=0.25.0
