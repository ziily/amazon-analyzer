# ============================================================
# Amazon 产品竞争力分析 v2 - 依赖清单（精简版）
# ============================================================
# 默认快速层只需要以下依赖，总安装时间 < 60 秒
# 体积约 200MB（vs 原版 2GB+）
# ============================================================

streamlit>=1.30.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0
matplotlib>=3.7.0
Pillow>=10.0.0
requests>=2.31.0
pyarrow>=14.0.0  # 用于 parquet 缓 
torch>=2.0.0 

# ============================================================
# 可选依赖：仅在启用深度分析（CLIP 图片分析）时才需要
# 默认不安装可节省 1.5GB+ 空间和大量额度
# 如需启用，取消下面三行注释
# ============================================================
torch>=2.0.0
# transformers>=4.36.0
# # 注意: CLIP 模型首次加载会下载 ~600MB

# ============================================================
# 已移除的依赖（快速层不再需要）
# ============================================================
# gliner                    # GLiNER2，1GB+，已被关键词方案替代
# psutil                    # 不再监控内存
# scikit-learn              # 未使用
# xlrd / openpyxl           # 未使用
